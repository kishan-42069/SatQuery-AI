# SatQuery AI — pinned backend dependencies

# API & Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
websockets==13.1
httpx==0.27.2

# Config & Validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Database (PostgreSQL + PostGIS)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
geoalchemy2==0.15.2
alembic==1.13.3

# Cache & Job Queue
redis==5.1.1
tenacity==8.5.0

# Agent Framework
langgraph==0.2.35
langchain==0.3.3
langchain-core==0.3.12
langchain-openai==0.2.3   # GPT-4o orchestrator (best production LLM)

# ── MODEL INJECTION POINT ────────────────────────────────────────────────────
# VLM/VQA model packages are listed below as stubs.
# Swap the specific model checkpoint via MODEL_NAME in your .env when ready.
# ─────────────────────────────────────────────────────────────────────────────
torch==2.4.1
torchvision==0.19.1
transformers==4.45.2
accelerate==0.34.2
sentencepiece==0.2.0

# Computer Vision
opencv-python-headless==4.10.0.84
Pillow==10.4.0
numpy==2.0.2
scipy==1.14.1

# Geospatial / GIS (GDAL installed via Dockerfile apt-get)
rasterio==1.3.11
geopandas==1.0.1
shapely==2.0.6
pyproj==3.6.1
fiona==1.10.1
affine==2.4.0

# Gemini VLM (Specialist Agents — Option A integration from satquery_backend)
# If GEMINI_API_KEY is not set, GeminiVisionClient runs in offline/heuristic mode.
google-genai>=1.4.0

# Logging & Observability
structlog==24.4.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
